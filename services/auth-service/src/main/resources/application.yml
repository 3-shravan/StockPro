spring:
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:services/auth-service/.env[.properties]
  application:
    name: ${APP_NAME:auth-service}
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: ${JPA_FORMAT_SQL:true}
  jackson:
    serialization:
      write-dates-as-timestamps: false
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false
  main:
    banner-mode: "off"

server:
  port: ${SERVER_PORT:8081}

# Eureka Discovery
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka/}

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:your_jwt_secret_at_least_32_chars}
  expiration: ${JWT_EXPIRATION:86400000}

# CORS
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}

# Logging
logging:
  level:
    root: WARN
    org.springframework: WARN
    org.apache: WARN
    com.zaxxer: WARN
    org.hibernate: WARN
    com.stockpro: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} %highlight(%-5level) %cyan(%logger{1}) : %msg%n"

# Actuator & Info
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized
      enabled: true
  info:
    env:
      enabled: true

info:
  app:
    name: ${spring.application.name}
    version: 1.0.0

# Internal Gateway Security
stockpro:
  security:
    internal-secret: ${INTERNAL_GATEWAY_SECRET:your_internal_gateway_secret}

# Springdoc / OpenAPI Custom Path
springdoc:
  api-docs:
    path: /auth/v3/api-docs
