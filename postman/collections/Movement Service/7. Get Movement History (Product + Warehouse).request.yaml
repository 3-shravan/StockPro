$kind: http-request
name: 7. Get Movement History (Product + Warehouse)
url: "{{base_url}}/api/movements/history/101/1"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 7000
