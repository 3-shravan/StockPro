$kind: http-request
name: 5. Change Password
url: "{{base_url}}/auth/password/1"
method: PUT
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "newPassword": "shantanu"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 5000
