server:
  port: ${SERVER_PORT:8761}

spring:
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:discovery-server/.env[.properties]
  application:
    name: discovery-server
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

logging:
  level:
    com.stockpro: INFO
